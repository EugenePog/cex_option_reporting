"""Bybit connector — placeholder.

To add Bybit: implement `BybitConnector(BaseCexConnector)` here (or in connector.py), set
`cex_code = "BYBIT"`, decorate with `@register`, and add a `mappers.py`. Nothing downstream
(ingestion, pipelines, web) needs to change — that's the point of the connector abstraction.
"""
